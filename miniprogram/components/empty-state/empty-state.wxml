<view class="empty">
  <view class="empty-icon"></view>
  <view class="empty-title">{{title}}</view>
  <view wx:if="{{desc}}" class="empty-desc">{{desc}}</view>
  <view wx:if="{{actionText}}" class="empty-btn" bindtap="onAction">{{actionText}}</view>
</view>
